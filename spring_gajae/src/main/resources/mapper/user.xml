<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="user">

	<select id="getUser" parameterType="map" resultType="map">
		SELECT *
		FROM MBLOG.USER2
	</select>

	<update id="userDeactivate">
		UPDATE MBLOG.USER2
		SET
		USER_STATUS = 0
		<if test="USER_ID != null">
			,USER_ID = #{USER_ID}
		</if>
	</update>

	<insert id="register">INSERT INTO MBLOG.USER2
		(
		USER_ID ,
		USER_NUMBER ,
		USER_PW ,
		USER_NAME ,
		USER_NICKNAME ,
		USER_EMAIL ,
		USER_MOBILE ,
		USER_BIRTH ,
		USER_ADDRESS ,
		USER_GENDER ,
		USER_STATUS
		)
		VALUES(
		#{USER_ID},
		USER_NUMBER.NEXTVAL,
		#{USER_PW},
		#{USER_NAME},
		#{USER_NICKNAME},
		#{USER_EMAIL},
		#{USER_MOBILE},
		#{USER_BIRTH},
		#{USER_ADDRESS},
		#{USER_GENDER},
		1
		)
	</insert>
</mapper>