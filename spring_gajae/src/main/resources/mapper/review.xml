<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "review" >
	 <insert id="reviewInsert" parameterType="map">
	 	INSERT INTO REVIEW(
	 		REVIEW_NUMBER, 
			R_NUMBER, 
			P_ID, 
			REVIEW_TITLE, 
			REVIEW_GOOD, 
			REVIEW_BAD, 
			REVIEW_DATE, 
			USER_NICKNAME, 
			REVIEW_PHOTO, 
			REVIEW_SERVICE, 
			REVIEW_FACILITY, 
			REVIEW_CLEAN, 
			REVIEW_LOCATION,
			REVIEW_COST 
			) VALUES (
			SEQ_REVIEW_NUMBER.NEXTVAL, 
			SEQ_R_NUMBER.NEXTVAL,
			SEQ_PROPERTY_NUMBER.NEXTVAL, 
			#{REVIEW_TITLE}, 
			#{REVIEW_GOOD}, 
			#{REVIEW_BAD}, 
			#{REVIEW_DATE}, 
			#{USER_NICKNAME}, 
			#{REVIEW_POTO}, 
			#{REVIEW_SERVICE}, 
			#{REVIEW_FACILITY}, 
			#{REVIEW_CLEAN}, 
			#{REVIEW_LOCATION}, 
			#{REVIEW_COST}
			)
	 </insert>
	 
	 <select id="reviewList" parameterType="java.util.HashMap" resultType="map">
	 	SELECT REVIEW_TITLE, REVIEW_GOOD, REVIEW_BAD, REVIEW_DATE
	 	FROM GAJAE.REVIEW
	 </select>
	 
	 <delete id="reviewDelete" parameterType="map">
	 	DELETE FROM REVIEW
	 	WHERE REVIEW_NUMBER = #{REVIEW_NUMBER}
	 </delete>
	</mapper>